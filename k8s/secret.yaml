apiVersion: v1
kind: Secret
metadata:
  name: which-form-secret
  namespace: which-form
type: Opaque
data:
  # Base64 encoded values
  # Example: echo -n "your-secret-value" | base64
  database-url: ""
  api-key: ""